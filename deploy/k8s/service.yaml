# REFERENCE ONLY — not applied on Replit.
apiVersion: v1
kind: Service
metadata:
  name: tkd-services-api
spec:
  selector:
    app: tkd-services-api
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP
