# REFERENCE ONLY — not applied on Replit. Illustrates a Kubernetes Deployment
# for the API server on a self-managed cluster.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tkd-services-api
  labels:
    app: tkd-services-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: tkd-services-api
  template:
    metadata:
      labels:
        app: tkd-services-api
    spec:
      containers:
        - name: api-server
          image: tkd-services-api:latest
          ports:
            - containerPort: 8080
          env:
            - name: NODE_ENV
              value: "production"
            - name: PORT
              value: "8080"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: tkd-services-secrets
                  key: database-url
            - name: SESSION_SECRET
              valueFrom:
                secretKeyRef:
                  name: tkd-services-secrets
                  key: session-secret
          readinessProbe:
            httpGet:
              path: /api/healthz
              port: 8080
            initialDelaySeconds: 5
          livenessProbe:
            httpGet:
              path: /api/healthz
              port: 8080
            initialDelaySeconds: 10
